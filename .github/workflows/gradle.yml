name: Build with Gradle

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Build'
        required: true
        default: 'Build'
      home:
        description: 'location'
        required: false
        
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 8
      uses: actions/setup-java@v1
      with:
        java-version: 8
    - name: Gradle Command
      uses: eskatos/gradle-command-action@v1.3.3
      with:
        gradle-version: 6    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradle clean assembleRelease --refresh-dependencies
    - name: Archive artifacts (Geyser Android)
      uses: actions/upload-artifact@v2
      if: success()
      with:
          name: Geyser Android
          path: /
